#ifndef SRC_UTILS_HPP
#define SRC_UTILS_HPP

#include <curses.h>

#include <term.h>

// Utils about curses

// cchar_t get_cchar(wchar_t, attr_t, short, void * = nullptr) noexcept;

// int mvaddwch(int, int, cchar_t) noexcept;

// cchar_t to_cchar(wchar_t )


#endif // SRC_UTILS_HPP